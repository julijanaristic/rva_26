<Window x:Class="AirportTerminalMonitoring.WPF.Views.ActivityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AirportTerminalMonitoring.WPF.Views"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"        
        mc:Ignorable="d"
        Title="ActivityWindow" Height="450" Width="800">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <TextBox Width="200" Margin="5" Text="{Binding SearchText}"/>
            <Button Content="Search" Margin="5" Command="{Binding SearchCommand}"/>
            <Button Content="Add" Margin="5" Command="{Binding AddCommand}"/>
            <Button Content="Edit" Margin="5" Command="{Binding EditCommand}"/>
            <Button Content="Delete" Margin="5" Command="{Binding DeleteCommand}"/>
            <Button Content="Undo" Margin="5" Command="{Binding UndoCommand}"/>
            <Button Content="Redo" Margin="5" Command="{Binding RedoCommand}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Activities}" SelectedItem="{Binding SelectedActivity}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Passengers" Binding="{Binding PassengerCount}"/>
                <DataGridTextColumn Header="Wait Time" Binding="{Binding AverageWaitTime}"/>
                <DataGridTextColumn Header="Delays" Binding="{Binding NumberOfDelays}"/>
                <DataGridTextColumn Header="Collection Time" Binding="{Binding CollectionTime}"/>
                <DataGridTextColumn Header="State" Binding="{Binding State}"/>
            </DataGrid.Columns>
        </DataGrid>

        <lvc:CartesianChart Series="{Binding Series}" XAxes="{Binding XAxes}"/>
    </DockPanel>
</Window>
